package main

func main() {
	// Go通过类型别名和结构体的形式支持用户自定义类型，或者叫定制类型
	// 一个带属性的结构体视图表示一个现实世界中的实体。结构体是复合类型，
	// 当需要定义一个类型，它由一系列属性组成，每个属性都有自己的类型和值的时候，就应该使用结构体
	// 它把数据聚集在一起，然后访问这些数据，就好像它是一个独立实体的一部分
	// 结构体也是值类型，因此可以通过new函数来创建

	// 组成结构体类型的那些数据称为字段，每个字段都有一个类型和名字，在一个结构体重，字段名字必须是唯一的。

	// 因为Go语言中没有类的概念，因此结构体有着更为重要的地位
}