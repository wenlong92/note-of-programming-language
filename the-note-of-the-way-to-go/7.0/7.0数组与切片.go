package main

func main() {
	// 本章开始剖析容器
	// 它可以是包含大量条目的数据结构
	// 例如数组、切片和map
	// Go明显受到了Python的影响

	// Go中拥有切片类型(slice),这是建立在Go语言数组类型之上的抽象
	// 要理解切片先理解数组
	// 数组在Go语言中不是特别常见，相反，切片却随处可见，它们构建在数组之上并提供更强大的能力和便捷。
}